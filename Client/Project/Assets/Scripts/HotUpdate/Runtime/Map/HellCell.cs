using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum eCellTypes
{
    None        = 0,
    Grass       = 1,
    Lake        = 2,
    Mountine    = 3,
}

public class HellCell : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
